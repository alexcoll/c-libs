#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <assert.h>

#define True 1

int test_llist_init() {
    assert(True);
    return 0;
}

int test_llist_print() {
    return 0;
}

int test_llist_add() {
    return 0;
}

int test_llist_exists() {
    return 0;
}

int test_llist_remove() {
    return 0;
}

int test_llist_get_ith() {
    return 0;
}

int main () {
    assert(test_llist_init());
    assert(test_llist_print());
    assert(test_llist_add());
    assert(test_llist_exists());
    assert(test_llist_remove());
    assert(test_llist_get_ith());
}
